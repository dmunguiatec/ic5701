apply plugin: 'groovy'
apply plugin: 'idea'

version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.antlr:antlr4-runtime:4.3'
}

sourceSets {
    main {
        groovy {
            srcDir 'src/main/java'
            srcDir 'src/main/groovy'
        }
    }
}